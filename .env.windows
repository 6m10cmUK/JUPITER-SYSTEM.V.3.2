# JUPITER NOTIFIER CLIENT 設定ファイル (Windows用)
# このファイルを .env にリネームして使用してください

# WebSocketサーバーのURL設定
# NorthflankでホストされているBotサーバーに接続する場合
WS_SERVER_URL=wss://site--jupiter-system--6qtwyp8fx6v7.code.run

# ローカルテスト用（同じPC上でBotを実行している場合）
# WS_SERVER_URL=ws://localhost:8080

# LAN内の別PCでBotを実行している場合の例
# WS_SERVER_URL=ws://192.168.1.100:8080

# その他の設定（将来の拡張用）
# NOTIFICATION_SOUND=true
# AUTO_RECONNECT=true
# RECONNECT_INTERVAL=5000