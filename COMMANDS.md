# JUPITER-SYSTEM.V.3.2 - Discordボットコマンド使用方法

Call of Cthulhu TRPG用のDiscordボットのコマンド使用方法を詳しく説明します。

## 🎲 ダイスロール関連

### `/roll [式]` - 基本ダイスロール
各種ダイス記法に対応したダイスロールコマンドです。

**基本的なダイス記法**
```
/roll 1d100        # 1～100の範囲で1回ダイス
/roll 2d6+3        # 6面ダイスを2回振って3を加算
/roll 3d6          # 6面ダイスを3回振る
```

**CoC専用記法**
```
/roll CCB<=80      # CoC技能判定（80以下で成功）
/roll CC<=80       # 通常のCoC判定
/roll CBR(3,80)    # 連続ロール（3回、技能値80）
/roll RES()        # 抵抗表ロール
```

**特殊ダイス**
```
/roll 3U6[5]       # 上方無限（6面で5以上を無限に振り続け）
/roll 10B6>=4      # バトルテック式（6面10個で4以上の成功数）
/roll D66          # D66記法（十の位と一の位を別々に）
```

**数式計算**
```
/roll C(10-4*3/2+2)  # 複雑な計算式の評価
```

### メッセージ直接入力
コマンドを使わずにチャットでダイス記法を直接入力することも可能です。
```
1d100
CCB<=80
2d6+3
```

## 👤 キャラクター関連

### `/status [type] [name] [custom]` - ステータス生成
CoC用のキャラクターステータスをランダム生成します。

**基本的な使用方法**
```
/status type:6版           # 6版ルール用ステータス生成
/status type:7版           # 7版ルール用ステータス生成
/status type:6版 name:田中探偵  # 名前付きで生成
/status type:7版 custom:true   # カスタムセットメニュー表示
```

**オプション**
- `type`: 必須。6版または7版を選択
- `name`: 任意。キャラクター名を指定
- `custom`: 任意。trueにするとカスタムセット機能を表示

**生成される能力値**
- **6版**: STR, CON, POW, DEX, APP, SIZ, INT, EDU, HP, MP, SAN
- **7版**: STR, CON, POW, DEX, APP, SIZ, INT, EDU, HP, MP, SAN, Luck

### `/feature [count]` - ランダム特徴生成
CoC用のランダム特徴を生成します。

```
/feature              # 1個の特徴を生成
/feature count:2      # 2個の特徴を生成
/feature count:3      # 3個の特徴を生成（最大）
```

**パラメータ**
- `count`: 任意。1-3の範囲で特徴の数を指定（デフォルト1個）

### `/name [type] [region] [count]` - 名前生成
TRPGキャラクター用の名前をランダム生成します。

```
/name type:日本人 region:関東 count:3
```

## 🎮 ゲーム関連

### `/job` - 職業検索・生成
CoC用の職業データベースを検索・表示します。

**サブコマンド一覧**
```
/job name query:探偵         # 職業名で検索
/job skill query:図書館      # 職業技能で検索  
/job point query:EDU         # 職業ポイントで検索
/job all                     # 全職業一覧表示
/job random count:3          # ランダムで3つの職業を表示
```

**検索例**
- `name`: 「探偵」「医師」「学者」など職業名で検索
- `skill`: 「図書館」「医学」「拳銃」など技能名で検索
- `point`: 「EDU」「STR+CON」など職業ポイントで検索
- `random count`: 1-8の範囲でランダム表示数を指定

### `/ninpo` - シノビガミ忍法検索
シノビガミ用の忍法データベースを検索します。

**サブコマンド一覧**
```
/ninpo name query:火遁        # 忍法名で検索
/ninpo type category:汎用忍法 query:攻撃    # 種別で検索
/ninpo specialty category:斜歯忍群 query:忍術  # 指定特技で検索
/ninpo all category:鞍馬神流  # カテゴリ別一覧
/ninpo effect query:ダメージ  # 効果説明で検索
```

**利用可能カテゴリ**
- 汎用忍法
- 斜歯忍群
- 鞍馬神流
- ハグレモノ
- 比良坂機関
- 私立御斎学園
- 隠忍の血統

### `/choice [args]` - 選択肢ランダム選択
複数の選択肢からランダムに1つを選びます。

```
/choice args:りんご みかん ばなな
/choice args:A案 B案 C案 D案
/choice args:戦う 逃げる 隠れる
```

**使用方法**
- `args`: 空白で区切った選択肢を入力
- 複数の選択肢の中からランダムに1つが選ばれます

### `/wordle` - 日本語Wordleゲーム
日本語版のWordleゲームを楽しめます。

```
/wordle  # ゲーム開始
```

### `/r6s [count]` - R6Sオペレーター選出
Rainbow Six Siegeのオペレーターをランダム選出します。

```
/r6s              # 1人のオペレーターを選出
/r6s count:5      # 5人のオペレーターを選出
```

## ⚙️ ユーティリティ

### `/schedule` - 通知スケジュール
定期通知や予定管理機能です。

```
/schedule          # スケジュール機能を表示
```

### `/densuke [date]` - カレンダー表示
指定日付のカレンダーを表示します。

```
/densuke date:2024-01-15   # 指定日のカレンダー表示
```

### `/table` - テーブル管理統合コマンド ⭐NEW v3.2.4
CoCシナリオ運営に特化したカテゴリ・ロール・チャンネルの統合管理コマンドです。

#### `/table setup [name] [handout]` - テーブルセットアップ
カテゴリ・ロール・チャンネルを一括でセットアップします。

```
/table setup name:狂気山脈 handout:3
```

**作成される要素**
- **カテゴリ**: 指定した名前のカテゴリ
- **ロール**: `{カテゴリ名}_1`、`{カテゴリ名}_通過者`
- **基本チャンネル**: 概要、日程、第1陣、通過者
- **HOチャンネル**: ho-1〜ho-{handout}（通過者のみアクセス可）

#### `/table handout [user] [number] [display_name]` - ハンドアウト割り当て
ユーザーにハンドアウトチャンネルへのアクセス権を付与し、専用チャンネルを作成します。

```
/table handout user:@ユーザー名 number:1 display_name:田中探偵
/table handout user:@ユーザー名 number:2  # display_name省略時はサーバープロフィール名
```

**機能**
- ho-{number}チャンネルへのアクセス権付与
- 専用チャンネル作成（表示名 or サーバープロフィール名）
- 通過者ロールも専用チャンネルにアクセス可能

#### `/table add [party_number]` - 陣営追加
新しい陣営（第n陣）を作成します。

```
/table add party_number:2  # 第2陣を作成
```

**作成される要素**
- **ロール**: `{カテゴリ名}_{party_number}`
- **チャンネル**: `第{party_number}陣`
- **権限**: 作成したロール + 通過者ロールがアクセス可能

#### `/table delete` - テーブル削除
現在のカテゴリを完全削除します。

```
/table delete  # カテゴリ内で実行（ID入力不要）
```

**安全機能**
- 詳細確認画面表示（削除内容の詳細）
- 確認ボタンでの最終確認
- キャンセル機能

**⚠️ 注意**: 全てのチャンネル・ロール・メッセージが削除されます（取り消し不可）

#### 使用要件
- **権限**: 管理者またはサーバーオーナーのみ
- **実行場所**: `handout`、`add`、`delete`はカテゴリ内のチャンネルから実行

## 🔧 開発・管理コマンド

### `/#setup [option]` - コマンドセット登録
ボットコマンドをサーバーに登録します。

```
/#setup          # 基本コマンドセットを登録
```

**注意**: 管理者権限が必要です。

### `/#update` - コマンド更新
登録済みコマンドを更新します。

```
/#update         # コマンドを最新版に更新
```

### `/#add [command]` - コマンド追加
特定のコマンドを追加登録します。

```
/#add command:roll    # rollコマンドを追加
```

## 🎯 使用例とコツ

### ダイスロールの組み合わせ例
```
/roll 1d100    # 基本的なパーセンタイル
CCB<=60        # チャットで技能判定（60%）
/roll 2d6+1d4  # 複数種類のダイスの組み合わせ
/roll C(3*2+5) # 数式での計算
```

### キャラクター作成フロー
```
1. /status type:7版 name:キャラ名
2. /feature count:2              # 特徴を2つ生成
3. /job random count:3           # 職業候補を3つ表示
4. /choice args:職業A 職業B 職業C # 職業をランダム選択
```

### セッション中の活用
```
/roll CCB<=80     # 技能判定
/choice args:A B C # プレイヤーの選択肢決定
/densuke          # 今日のカレンダー確認
```

## 🛡️ 注意事項

### 権限について
- 一般ユーザー: ダイス、検索、ゲーム系コマンドが使用可能
- 管理者: カテゴリ操作、コマンド管理機能が追加で使用可能

### 制限事項
- ダイスの最大数や範囲に制限があります
- 一部のコマンドには実行頻度制限があります
- 検索結果は表示数に上限があります

### エラーが発生した場合
- コマンドの入力形式を確認してください
- 必須パラメータが不足していないか確認してください
- サーバーの権限設定を確認してください

---

**このボットは Call of Cthulhu TRPG とシノビガミのセッション支援を目的として設計されています。**